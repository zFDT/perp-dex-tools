# 多交易对配置文件使用指南

## 📁 目录结构

```
configs/
├── backpack_buy_multi.json       # 多币种买入配置
├── backpack_sell_multi.json      # 多币种卖出配置
└── backpack_mixed_strategy.json  # 混合策略配置
```

## 🚀 快速开始

### 方法 1: 使用启动脚本（推荐）

```powershell
# Windows PowerShell
.\start_multi.ps1 backpack_sell_multi
```

### 方法 2: 直接运行

```powershell
python runbot_multi.py --config configs\backpack_sell_multi.json
```

## 📝 预置配置说明

### 1. backpack_buy_multi.json
- **策略**: 多币种买入
- **交易对**: SOL, BTC, ETH
- **方向**: 全部做多 (buy)
- **参数**: 统一的止盈 0.02%，网格步长 0.2%

### 2. backpack_sell_multi.json
- **策略**: 多币种卖出
- **交易对**: SOL, BTC, ETH, SUI
- **方向**: 全部做空 (sell)
- **参数**: 统一的止盈 0.02%，网格步长 0.2%

### 3. backpack_mixed_strategy.json
- **策略**: 混合策略（不同币种不同参数）
- **SOL**: 卖出，激进策略（网格 0.15%，15 单）
- **BTC**: 买入，保守策略（网格 0.3%，止盈 0.03%，8 单）
- **ETH**: 卖出，中等策略（网格 0.2%，12 单）
- **SUI**: 买入，激进策略（网格 0.1%，20 单）

## ⚙️ 自定义配置

### 基本结构

```json
{
  "exchange": "交易所名称",
  "env_file": ".env",
  "common_params": {
    "公共参数": "应用到所有交易对"
  },
  "tickers": [
    {
      "ticker": "交易对",
      "quantity": "数量",
      "instance_id": "实例ID",
      "其他参数": "覆盖公共参数"
    }
  ]
}
```

### 可用参数

#### 公共参数 (common_params)
- `take_profit`: 止盈百分比（默认 0.02）
- `direction`: 交易方向 "buy" 或 "sell"（默认 "buy"）
- `max_orders`: 最大订单数（默认 40）
- `wait_time`: 等待时间秒数（默认 450）
- `grid_step`: 网格步长百分比（默认 -100）
- `stop_price`: 停止价格（默认 -1）
- `pause_price`: 暂停价格（默认 -1）
- `boost_mode`: Boost 模式（默认 false）

#### 必填参数 (每个交易对)
- `ticker`: 币种代码（如 "SOL", "BTC"）
- `quantity`: 交易数量
- `instance_id`: 实例标识符（用于日志）

#### 可选参数 (每个交易对)
任何 `common_params` 中的参数都可以在具体交易对中覆盖

## 💡 使用示例

### 示例 1: 简单的多币种卖出

创建 `my_sell.json`:

```json
{
  "exchange": "backpack",
  "common_params": {
    "direction": "sell",
    "take_profit": 0.02,
    "grid_step": 0.2
  },
  "tickers": [
    {"ticker": "SOL", "quantity": 0.5, "instance_id": "SOL_1"},
    {"ticker": "BTC", "quantity": 0.01, "instance_id": "BTC_1"}
  ]
}
```

运行:
```powershell
python runbot_multi.py --config my_sell.json
```

### 示例 2: 不同参数的混合策略

创建 `my_mixed.json`:

```json
{
  "exchange": "backpack",
  "tickers": [
    {
      "ticker": "SOL",
      "quantity": 0.5,
      "instance_id": "SOL_aggressive",
      "direction": "sell",
      "take_profit": 0.015,
      "grid_step": 0.1,
      "max_orders": 20
    },
    {
      "ticker": "BTC",
      "quantity": 0.01,
      "instance_id": "BTC_conservative",
      "direction": "buy",
      "take_profit": 0.03,
      "grid_step": 0.5,
      "max_orders": 5
    }
  ]
}
```

### 示例 3: 使用价格控制

```json
{
  "exchange": "backpack",
  "common_params": {
    "direction": "buy",
    "stop_price": 100000,
    "pause_price": 95000
  },
  "tickers": [
    {"ticker": "BTC", "quantity": 0.01, "instance_id": "BTC_1"}
  ]
}
```

## 📊 日志文件

运行后会在 `logs/` 目录生成对应的日志文件：

```
logs/
├── backpack_SOL_sell_orders.csv
├── backpack_BTC_buy_orders.csv
└── backpack_ETH_sell_orders.csv
```

文件命名格式: `{交易所}_{币种}_{实例ID}_orders.csv`

## 🔔 飞书通知

每个交易对会独立发送飞书通知，包含：
- 启动通知（包含配置信息）
- 错误通知（出错时）
- 每小时统计（交易数据）

通知中会显示对应的 `instance_id` 以便区分。

## ⚠️ 注意事项

1. **Instance ID 唯一性**: 确保每个交易对的 `instance_id` 不同
2. **API 限流**: 同时运行多个交易对可能触发交易所 API 限流
3. **资源占用**: 更多交易对 = 更多 CPU/内存占用
4. **Boost 模式**: 仅支持 `backpack` 和 `aster` 交易所
5. **JSON 格式**: 确保 JSON 格式正确（可使用在线验证器）
6. **启动延迟**: 各交易对启动间隔 2 秒，避免冲突

## 🛠️ 故障排除

### JSON 解析错误
- 检查 JSON 格式是否正确
- 确保所有引号、逗号、括号匹配
- 最后一项后不要有多余逗号

### 配置文件未找到
- 检查文件路径是否正确
- 确保文件在 `configs/` 目录或当前目录

### 交易对启动失败
- 检查 `ticker` 和 `quantity` 是否正确
- 确保 `.env` 文件中有正确的 API 凭证
- 查看具体的错误信息

## 📚 更多信息

详细配置说明请参考 [CONFIG_GUIDE.md](../CONFIG_GUIDE.md)

## 🤝 从命令行迁移

之前的命令行方式:
```bash
python runbot.py --exchange backpack --ticker SOL --quantity 0.5 --direction sell
```

现在创建配置文件:
```json
{
  "exchange": "backpack",
  "tickers": [
    {"ticker": "SOL", "quantity": 0.5, "instance_id": "SOL_1", "direction": "sell"}
  ]
}
```

然后运行:
```bash
python runbot_multi.py --config my_config.json
```

## ✨ 优势

使用配置文件的好处：
- ✅ 同时管理多个交易对
- ✅ 配置可复用和版本控制
- ✅ 避免长命令行参数
- ✅ 支持复杂的混合策略
- ✅ 易于分享和备份配置
